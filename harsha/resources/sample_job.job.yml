resources:
  jobs:
    harsha_job:
      name: harsha_job

      trigger:
        periodic:
          interval: 1
          unit: DAYS

      tasks:
        - task_key: run_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.harsha_etl.id}