bundle:
  name: harsha
  uuid: c91bfb92-179d-41bb-b26a-d0552c0d966a

include:
  - resources/*.yml

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-694533b1-7860.cloud.databricks.com
      root_path: /Workspace/Shared/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: ${workspace.current_user.short_name}
    permissions:
      - group_name: users        # 🔥 THIS IS THE REAL FIX
        level: CAN_MANAGE

  prod:
    mode: production
    workspace:
      host: https://dbc-694533b1-7860.cloud.databricks.com
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - group_name: users        # 🔥 ADD HERE ALSO
        level: CAN_MANAGE
      - user_name: harshavardhanchari0304@gmail.com
        level: CAN_MANAGE